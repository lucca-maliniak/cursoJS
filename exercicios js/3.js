/*Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente*/

const calcularPot = (base, exp) => Math.pow(base, exp)
console.log(calcularPot(3, 2))
console.log(calcularPot(5, 2))
console.log(calcularPot(5, 3))